# handoff.md — 收工交接

> 規範與資料夾結構看 `AGENTS.md`；這份只記「**現在做到哪、下一步做什麼**」。
> 每次收工由當班 agent 更新，最後一節必填。

---

## ⏯️ 目前做到哪（2026-08-05 更新）

**本輪（08-05）完成四件，全部已上線（`main` = `4467af0`）。
其中 D 的教學內容尚未經老師審核——依 AGENTS.md 5.4，未審過不算完成。**

### A. 講義練習題號 → exam 直達（老師要求「另開分頁避免迷路」）

139 個 111–115 題號變成可點連結，點下去跳到 exam 對應題、自動切年份、捲到該題並高亮 3.2 秒。

⚠️ **只有 52% 連得到**：講義引用 106–115 共 266 個題號，exam 只收 111–115。
106–110 那 127 個維持純文字（同一行會有的能點有的不能點，這是已知取捨）。

改了 `exam/`（源頭在本機外部，**已先整份備份**到
`D:\大考複習\歷屆試題詳解\互動網頁版_備份_20260805_2123`）。踩到兩個坑見「注意事項」。

### B. 58 個考點的考頻星等補齊（方案 A 前置）

星等本來只畫在知識地圖 SVG 裡、程式讀不到。已抄成 `content/*.py` 的 `freq`。
分布：3★ 23 個、2★ 32 個、1★ 3 個。矩陣 kp3 由 ★★☆ 改 ★☆☆（老師定案）。

### C. 方案 A：Part 1「今天走哪條路」路線選擇器 ✅

| 路線 | 篩選規則 |
|---|---|
| 完整複習（預設） | 全部顯示、原順序 |
| 先拿基本分 | 只留 3★，依官方答對率**由高到低**排（老師定案「3★ 全收、難的排後面」） |
| 錯題回補 | 只留自評／小測判定「待複習」的考點 |

**為什麼不用答對率當篩選條件**：答對率量的是「題目難度」不是「考點階層」。
正餘弦定理 29% 是那幾年題目出得難，不代表它不是基本分——拿它篩會誤傷基礎題材。

選擇存 `localStorage(mm-route)` 跨單元沿用。帶 `#kpN` 錨點進頁一律走完整模式
（否則目標考點可能剛好被篩掉），但**不覆寫**學生存的路線。

### D. 方案 B 試點：多項式「入口三格 ＋ 入口診斷」⚠️ **教學內容待老師審**

**只做多項式一個單元當樣張**；其餘 10 單元沒有 `axes` 資料就完全不渲染，零影響。

三格：**式**（除法、餘式、係數與恆等關係）／**根**（根、因式、實根範圍與正負號）／
**圖**（交點、頂點、對稱、平移）。沿用 ChatGPT 試作版的分法——兩邊 5 個考點剛好 1:1 對應。

各考點的建議入口（**這是教學判斷，老師尚未審**）：

| 考點 | 主入口 | 次 | 我的理由 |
|---|---|---|---|
| kp1 除法餘式因式 | 式 | 根 | |
| kp2 二次函數判別式配方 | **圖** | 根 | 判別式＝圖形與 x 軸幾個交點、配方＝找頂點，用圖形觀點能把兩件事收在同一直覺下 |
| kp3 多項式方程式與不等式 | 根 | 圖 | |
| kp4 高次方程式有理根勘根 | 根 | 式 | |
| kp5 三次函數圖形對稱中心 | 圖 | 式 | |

入口診斷 6 題（**自編辨識練習、非官方原題，老師尚未審**）：2 題「式」、2 題「根」、2 題「圖」，
每題刻意只有一個明顯入口。**比原版強的地方**：ChatGPT 版是靜態查表，這裡做成可作答——
點選 → 即時對錯 → 答錯標出正解 → 一句話說明 → 錨點連回該考點；作答後鎖定避免亂點湊對。

---

## ⏮️ 上一輪（2026-08-03）

**本輪做完三件事：guide 子站上線、借鏡 ChatGPT 版的分析定案、答對率徽章（待審）。**

### 1. `guide/` 複習講義子站已合併並上線 ✅

`feat/guide-web-version` 已 **fast-forward 併回 main 並 push**（`main` = `2c730a0`）。
發布前完整驗證過（原本註記「未親自驗證」，本輪補驗）：

| 項目 | 結果 |
|---|---|
| 建置可重現 | 重跑 `build_guide.py` → **0 檔案差異**（正式檔＝單一來源產物） |
| KaTeX | 15 頁 **0 錯誤**；矩陣章 43 個 mtable 列數正確；渲染後 0 殘留原始 TeX |
| 圖片／互動 | 46 張圖 0 缺；490 處挖空的單點／全部顯示／全部隱藏都正常 |
| 連結／埋碼 | `../`、`../concept-map/`、前後章皆正確；GA＋Clarity＋OG 每頁齊全 |

總入口 `index.html` 已加第三張入口卡（`.entry.gd`，苔綠 `#4a7042`），
hero 與「建議用法」改寫成三套教材分工。**`guide/img/image31.png` 是孤兒檔（無頁面引用），暫不動。**

### 2. 借鏡 `D:\gmail\ChatGPT_複習` 的結論（三個單元都看過了）

該站有多項式 v2／直線與圓 v1／數列與級數 v1 三個獨立 Next.js 子站。
**內容比本站淺很多**（例題是同構重新設計、數學式用 Unicode 上下標非真排版、一個模組只有 1 題檢核），
**但它有本站沒有的「開始算之前那一步」鷹架**：

1. **每單元 3–4 格「核心表示」＋入口診斷**（多項式＝式/根/圖；直線與圓＝方向/方程/距離/區域；
   數列＝項/遞迴/和/函數模型）。8 張題幹卡只問「第一個表示」、明說不要計算。
   ——本站的解題線索地圖是**全站索引**（查表要先知道查什麼）；4 格是**先把題目分進 4 個抽屜**。對中等生抽屜比索引好用。**這是唯一真正該偷的東西。**
2. **三條路線**（完整／先拿基本分／錯題回補）——本站首頁一次攤 58 考點，中等生的第一反應是「看不完」。
3. **必會 vs 延伸兩層分流**（延伸預設收在 `<details>`）。
4. 半個：**四步命名腳本**（辨識→轉譯→求解→檢查）。本站 `worked` 的 do/why 其實更好（有「為什麼」），差在沒有命名階段，尤其「轉譯」這步被埋在 `do` 裡。

**該守的**：① 官方原卷校正的正確性與深度 ② 一張網 vs 三座孤島（該站每單元一個獨立專案、彼此不連，
新框架必須連進現有三張地圖，不准再開獨立頁）③ 單一來源 build，不改技術棧。

**老師已排序**：`A（路線選擇器）→ B（多項式試點入口四格）→ 看反應再決定推廣 / C（分層）`。
方案 A 的隱藏前置：58 個考點的 ★ 目前只在知識地圖 SVG 文字裡，`content/*.py` 只有矩陣 5 個有 `freq` 鍵，要搬成結構化資料（**必須人眼複核**，見 A2 教訓）。

### 3. 考點 × 官方答對率徽章 ✅ **已上線**（2026-08-05）

**重要發現：答對率資料本來就在自家 `exam/`**，不必用 ChatGPT 版那四筆二手數字。
`exam/data.js` 200 題全有官方答對率，195 題已標好 `kaodian` 連回 concept-map 的 `#kp` 錨點（319 條）。

已併入 `main`（`6e49e7c`，已 push）：
- 新增 `build/gen_examrates.py`：`exam/data.js` → `content/exam_rates.py`（只讀不改 exam/）
- 只收 **n≥3** 的考點（33/52 組）；少於 3 題只能說「那一題難」，不能說「這個考點難」
- `build_html._rate_badge()` ＋ `style.css` 的中性灰藍 `.kp-rate`（不做紅綠判斷）

**多選口徑：老師 2026-08-05 定案採「全對率」**——與單選答對率同構念（完全答對的比例），
才能一起平均。得分率是部分給分，混著平均會灌水。
差距不小，例：`trig kp4`（3 題全是多選）全對率 **11.7%** vs 得分率 **30.7%**。
口徑寫在 `gen_examrates.MULTI_METRIC` 一個常數，日後要換只動這裡再重跑產線。

**更新流程**（`exam/` 日後有新題時）：
```
python build/gen_examrates.py   # 刷新快照
python build/build.py           # 重建 11 單元頁
逐檔列名複製 dist/ 的 11 個單元頁 → 驗矩陣頁 → commit
```

## ⏮️ 上一輪（2026-08-01 ~ 08-02）

**《任務書_SOIL認知優化_2026-07-10》已全部執行完畢並上線**（唯一未做項是 D1，老師主動說「先不做」）。

本輪（2026-08-01 ~ 08-02）依序完成、**每批都經老師逐項審核後才 push**：

| 階段 | 內容 | commit |
|---|---|---|
| 前置 | 直線與圓 114A多8 補「切線法」另解；多項式 113A單3 從 kp5 移到 kp4 | `113371f` `31e699b` |
| 體檢 A/B/C 包 | 164 題反向檢核：去課綱外工具（微分／積分／托勒密／arcsin／核空間）、補 4 個教學缺口、矩陣 3 題詳解補推導、12 處跨單元先備標註 | `1918a52` `3b06eff` `8ec34fa` |
| 選項對卷複查 | 91 題逐題對官方 PDF，修 4 處不符（含跨單元卡 113題8 選項是自創變體、答案連帶錯） | `72acc01` |
| SOIL A | A1 解題核心預設收合（先判斷再對照）／A2 Part3「帶走一句話」回想 | `d623868` `300bbdc` |
| SOIL B | B1 Part0 直覺挑戰（機率貝氏／三角 T2／直線與圓 L2）／B2 header 收合＋地圖觸控 | `1852922` `31d4c38` `4d6d1b7` |
| SOIL C1 | 確認理解全面升級 **2 題微集**（辨析＋變情境）：11 單元 58 考點 **116 題** | `4ab4e15`→`138f70b`（11 個 commit） |
| SOIL D | D4 題序打散／D3 地圖建議看法／D2 強調色統一／D5 渲染效能 | `dd39b63` `7ac95a3` `39d5f8e` `6937ec5` |

**最後一個動作（無程式碼變更）**：比較老師提供的 ChatGPT 試作版 docx
（`D:\gmail\ChatGPT_複習\試作版\學測數學A_多項式_學生自學版_試作.docx`）與本站多項式單元的優缺點，
產出「該偷的 5 件事／該守的 2 件事」與三個行動選項（A/B/C），**老師尚未定案**——見「下一步 3」。

## 🚦 目前狀態

- **可運行**：`concept-map/` 全站可 build、validate 綠燈；`guide/` 15 頁已驗（KaTeX 0 錯誤）。
- **已上線**：`main` = **`4467af0`**，**已 push**。含 SOIL 全部成果 ＋ `guide/` 子站 ＋ 總入口三張卡
  ＋ 33 個考點答對率徽章 ＋ 58 個考點星等 ＋ 路線選擇器 ＋ 講義題號直達 exam
  ＋ **D5 分塊漸進渲染修復** ＋ **方案 B 多項式試點**。
- **做一半的**：沒有程式面的。但**方案 B 的教學內容尚未經老師審**（見「下一步 2」）——
  依 AGENTS.md 5.4，未審過不算完成。
- **本輪的三方合流已驗**：D5 修復（另一 session）＋ 路線選擇器 ＋ 方案 B 三者
  同時作用無衝突。驗法是 **rebase 後重跑 build 比對產物零差異**——
  HTML 是產物，兩個 session 都重建過時不能只看 git 無衝突就放心。
- **D5 分塊失效已修**（`1d96bf5`）：`_mmChunks()` 改成遇到 `.wrap`／`.card` 都往內下探。
  該 session 是先 rebase 到 `171759b` 才重建，**路線選擇器沒被蓋掉**（已逐頁 diff 確認
  11 頁差異都只有 `_mmChunks` 那一段）。矩陣頁分塊數 11 → 137、最長單次凍結
  106ms → 21ms、首屏渲染完成 106ms → 1.4ms；代價是全頁總 CPU 107ms → 176ms。
- **分支狀況**：`main` = `4467af0`（＝`origin/main`，乾淨）；
  `feat/guide-web-version` = `87361e3` 已併入 main，**分支還留著沒刪**（怕有平行 session 還在用）。
- **平行作業**：這個 repo 常有多個 session 動**同一個工作目錄**。收工前務必
  `git rev-parse HEAD` 與 `origin/<branch>` 比對確認。

## ➡️ 下一步（具體、可執行）

1. **方案 A 的收尾（單元頁已完成，剩兩個延伸，都是可選的）**
   - `concept-map/index.html`（**手寫維護**）也放一組路線按鈕：學生是從那裡進站的，
     「看不完」的壓迫感最先發生在那頁。單元頁的 `mm-route` 可直接沿用。
   - 「先拿基本分」目前只篩 Part 1 考點卡；要不要連 Part 2 模擬實戰的題目一起篩，**未定**。

2. **⚠️ 最優先：老師審方案 B 的教學內容**（程式已上線，內容未審）
   - **5 個考點的入口指派**（尤其 kp2 取「圖」而非「根」，理由見上）。
   - **入口診斷 6 題的題幹與說明**（自編辨識練習，非官方原題）。
   - 審過再決定要不要推廣到其餘 10 單元；**每個單元的 3–4 格都要老師定**，AI 不能自己決定。
   - 推廣時可直接沿用 ChatGPT 版現成的：直線與圓＝方向/方程/距離/區域、
     數列＝項/遞迴/和/函數模型（那三個單元他們做過）。

3. **B1 直覺挑戰擴充到其餘 8 單元**（現只有機率／三角／直線與圓有）
   - 在 `content/<slug>.py` 的 `part0` 加 `challenge` 鍵
     （`q` / `options` / `answer`（**0-based**）/ `reveal` / `kp`），無資料的單元不渲染。
   - 每單元起草 2 個候選 → **給老師選** → build → 驗證三步互動與錨點。

4. **（老師尚未定）錯誤代碼 → 回補考點**：ChatGPT 版每單元有 4 類錯誤代碼（如直線與圓的 D/E/M/R），
   可接上現有待複習頁。等方案 A/B 上線看反應再決定。

5. ~~**修 D5 漸進渲染分塊失效**~~ **已完成**（`1d96bf5`）。留一個**可選**的後續：
   分塊變多後批次數 2 → 13，而 `schedule()` 的後備計時器是 250ms。前景分頁 rIC
   正常觸發時不受影響，但 rIC 被餓死時全頁渲染完的牆鐘時間會從約 0.5s 拉長到約 3s
   （首屏仍在第一批就完成，只影響「還沒捲到的下半頁」）。若要改，動的是後備計時器的
   間隔或 12ms 時間預算，**不可拿掉 rIC + setTimeout 雙保險本身**。

6. **（可選）答對率的延伸用法**：`content/exam_rates.py` 是結構化資料，
   除了考點徽章與路線排序，也能餵給概念地圖做難度熱度。

## ⚠️ 注意事項

**本輪新踩的坑（都已解決，但機制還在）：**
- **`part2_kp.py` 是位置制**：D4 改題序時必須用同一組置換同步改這張表，否則「解題核心回考點」全部錯位。
  `validate.py` 只擋長度不符、擋不了順序錯 → 改動後要逐題驗證 tag→kp 配對。
- **`rIC` / `rAF` 在離屏 iframe 與背景分頁會被餓死**：D5 的漸進渲染排程因此必須 `requestIdleCallback` + `setTimeout` 雙保險，
  只跑先到的那個。日後任何延遲工作都要照這個模式。
- **驗證要驗語意、不要只驗一致性**：A2 第一版用錯 regex，11 個單元全抽成徽章文字「必考核心」，
  而自動比對還「通過」（因為兩邊用同一支錯的 regex）。**抽取式的驗證一定要人眼看一次內容**。
- **KaTeX 高度會有假象**：驗矩陣式沒散掉要看 `.katex .mtable` 數量與 vlist 列數，不能只量 `getBoundingClientRect().height`。
- **效能優化要驗「機制真的生效」，不是只看端點數字**：D5 初版量到「首屏 833ms→0」就結案，
  但那是延後渲染換來的，分塊其實一塊都沒切（`_mmChunks()` 回傳 11、整個 `.wrap` 是一塊）。
  下次量效能請一併記**分塊數／批次數**這種能證明機制有跑的中間量。
- **預覽分頁是 `hidden`**：本環境的 Browser pane 不顯示，`document.visibilityState` 永遠是 hidden，
  `setTimeout` 被節流、`rIC` 被餓死 → **牆鐘時間量不準**。要比較效能請量
  `performance.now()` 夾出來的**同步 CPU 時間**（單次最長凍結／總 CPU），那個不受節流影響。
- **dist 內的樣張預覽**：KaTeX 路徑要改成 `../katex/` 才會渲染（`build_html` 產出的是 `katex/`）。
- **`build_conceptmap.py` 的 JS 寫在 Python 字串裡**：註解要用 `#`，寫 `//` 會是 Python 語法錯誤，
  而且失敗時若照舊複製 `dist/`，會把**舊檔**蓋成正式檔（發生過一次，已修）。

**本輪（08-05）新踩的：**
- **產物型檔案的合併不能只信 git**：`concept-map/115學測數學_*.html` 是 build 產物。
  兩個 session 都重建過時，git rebase 可能「無衝突」但產物其實是拼接的。
  正解是 **rebase 後重跑 build，用 `git status` 確認零差異**——一致才代表合併正確。
- **`_mmChunks` 的分塊是依 DOM 層級**：考點卡若被包進新容器，整批會塌成單一分塊。
  所以路線選擇器**不加容器**，直接在原位重排兄弟節點。（順帶發現分塊本來就失效，見下一步 5。）
- **篩選功能一定要處理「篩不到」**：第一版「錯題回補」在沒有標記的單元會讓 Part 1 整個空掉，
  學生翻到下一單元就是一片空白。現在一律退回全部顯示並說明原因。
- **錨點 vs 篩選會打架**：帶 `#kpN` 進頁時目標考點可能剛好被篩掉 → 跳過去是空的。
  解法是錨點進頁強制走完整模式，但**不能覆寫**學生存的路線。
- **手機觸控目標**：路線按鈕第一版只有 26px 高，手機按不準（學生主力裝置是手機）。
  已加 `min-height:40px`。日後新增任何按鈕都要量一次實際高度。
- **預覽窗格隱藏時無法驗捲動**：`document.hidden` 為 true 時瀏覽器不執行捲動，
  連手動 `window.scrollTo` 都是 0。要驗跳題那類行為，改用**同源 iframe** 從旁觀察 DOM。

**上一輪（08-03）踩的：**
- **`exam/data.js` 的 `rate` 有三種格式**，直接用 `^\d+%$` 解析會**默默丟掉 40% 的數 A 題**：
  單選／選填／混合＝`"29%"`；多選＝`"57%（全對率 37%）"`；非選擇題＝`"非選擇題（大考中心未列答對率）"`。
  多選那 30 題正是最有鑑別度的，漏掉會嚴重偏誤（覆蓋率從 33 組掉到 20 組）。
- **`build_guide.py` 直接寫進 `guide/`，不經 `dist/`**（與 concept-map 的 build→dist→逐檔複製慣例不同）。
  要驗可重現性就靠「工作區乾淨 → 重跑 → `git status` 看差異」，出事用 `git checkout` 還原。

**暫時 workaround / 已知不修：**
- D4 題序打散後，學生**既有**錯題紀錄的 `p2qN` 錨點會指到新位置的題（紀錄與題名都在，只是重測跳錯位置）。
  一次性影響、新紀錄正常，判定不值得做遷移機制。
- `任務書_SOIL認知優化_2026-07-10.md` 已加入 `.gitignore`（本機保留、不發布）。**別再 commit 它。**
- `exam/` 的源頭在本機外部 `D:\大考複習\歷屆試題詳解\互動網頁版\`，**不要在本 repo 內改 exam/**。

## 🕐 最後更新

- **時間**：2026-08-05 23:20（週三）
- **更新者**：Claude Code（Opus）@ Kitty
- **Git push 狀態**：
  - `main` = `4467af0` — **已 push**。含 SOIL 全部成果 ＋ `guide/` 子站 ＋ 總入口三張入口卡
    ＋ 答對率徽章 ＋ 58 考點星等 ＋ 路線選擇器 ＋ 講義題號直達 exam ＋ D5 分塊修復
    ＋ 方案 B 多項式試點（**教學內容待審**）。
  - `feat/guide-web-version` = `87361e3` — 已併入 main，遠端分支**還留著沒刪**（怕平行 session 還在用）。
  - D5 分塊那個平行 session 已完成並合流，三方成果同時作用已驗證。
  - ⚠️ 這個 repo 常有多 session 同時操作**同一個工作目錄**，收工前務必
    `git rev-parse HEAD` 與 `origin/<branch>` 比對確認。
