# 任務書：mathmap 網站 SOIL 認知優化（2026-07-10）

> **給接手的 AI（Claude Code session）**：這份文件是一次完整審查的產出，請把它當作本次工作的唯一需求來源。
> 審查方式：2026-07-10 以認知科學（SOIL 教學心法）架構實測已上線網站 https://mathmap-kitty.github.io/web/ ——逐頁走過總入口、重點整理目錄、「排列組合與機率」完整單元頁、概念地圖（依賴鏈視圖）、歷屆試題區（含逐步提示實測）、窄視窗（640px）表現。
> 使用者是游心怡（Kitty）老師，高中數學教師，網站給高三學生自學用。她要求直接、具體、不要空泛稱讚。

---

## 0. 開工前必讀與必做（不可跳過）

### 0.1 必讀文件（都在 repo 內）
1. `README.md`（repo 根目錄）——站台結構、兩個子站的更新方式
2. `concept-map/README.md` ——建置管線、開發慣例、**KaTeX CSS 大雷**
3. `concept-map/交接文件_給Claude_Code.md` ——互動元件（blank／sol-btn）規格

### 0.2 專案關鍵事實
- Repo：`D:\web`，git remote `https://github.com/mathmap-kitty/web.git`，GitHub Pages 部署。
- **concept-map/ 的內容源頭是 `concept-map/content/*.py`（每單元一檔，如 `prob.py`＝排列組合與機率；另有 `openers.py`、`cues.py`、`soil_maps.py`、`part2_kp.py`、`units.py` 等），版型與產生邏輯在 `concept-map/build/*.py`（`build_html.py` 為單元頁主版型）。**
- 建置流程：在 `concept-map/` 下執行 `python build/build.py` → 輸出到 `dist/` → 人工確認後覆蓋 `concept-map/` 內正式 `.html` → commit。
- **絕對不要直接改 `concept-map/` 根目錄的正式 .html 或 `dist/` 內的檔案**——改 content／build 的 py 檔，用 build 產出。
- `exam/` 子站源頭在 `D:\大考複習\歷屆試題詳解\互動網頁版\`，更新方式是整包覆蓋。**本任務書所有任務都不動 exam/**（它的逐步提示設計已是正確做法，還是本次修改的參考標準）。
- KaTeX 本地包（`concept-map/katex/`），行內 `\(...\)`。**CSS 大雷：不可用後代選擇器碰含數學容器內的 span（如 `.opts span`），會打散 KaTeX 排版；只能用子選擇器或專屬 class。** 任何 CSS 改動後，必須用「矩陣」單元頁目視驗證數學式沒有散掉。

### 0.3 開工第一步：管線健檢（先做這個，做完停下回報）
1. 在 `concept-map/` 執行 `python build/build.py`。
2. 比對 `dist/` 輸出與 `concept-map/` 正式檔（diff 主要單元頁即可）。
3. **若 diff 幾乎為零** → 管線是最新的，回報後開始批次 A。
4. **若 diff 很大** → 表示正式檔上有未回寫源頭的手改，**停下來**，列出差異請使用者裁決，不要自行推進。

### 0.4 工作規範（全程遵守）
- **一任務一成果**：一次只做一個任務編號，build＋驗證＋回報後，才進下一個。
- 動手前先提出簡短計畫（要改哪幾個檔、怎麼改），有歧義先問。
- 每個任務一個 commit，訊息格式：`SOIL優化 A1：<一句話>`。
- **完成一個批次後停下，請使用者確認再 push**；不可自行 push。
- 不刪檔案、不覆蓋源頭教材；`dist/` 覆蓋正式檔屬既定流程，可做。
- 每步回報格式：①做了什麼 ②改了哪些檔 ③如何驗證（含 KaTeX 目檢）④哪些地方需要老師確認。
- 涉及**教學內容**（B1、C1 的題目文字）：AI 起草，但**必須經老師審核才算完成**——教學判斷屬於老師。

---

## 1. 為什麼改（30 秒背景，讓你理解每個任務的目的）

網站宏觀架構已符合認知科學：單元頁 Part 0 引起動機 → Part 1 建構概念 → Part 2 模擬實戰 → Part 3 速查；考點內有先備知識、迷思澄清、互動填空（提取練習）、自評。**不要動這個大架構。**

要修的是幾個「認知代工」漏洞：學生該親自完成的認知步驟被網頁替他做掉了，以及認知負荷問題。核心原則一句話：**題型判斷（這題是哪類）是學測最大失分點，必須讓學生先判斷、再揭示；所有提示都應「先想再看」。**

---

## 2. 任務清單

### 批次 A（最小改動、最大效益）

#### A1｜歷屆試題與模擬實戰的「解題核心」改為預設隱藏 ★本次最重要
- **現況**：重點整理單元頁中，每題歷屆試題與模擬實戰題的題目上方直接印出「解題核心：貝氏定理（連續採檢的後驗機率比值）↑ 複習考點」。學生還沒讀題就被告知題型，Selection（判讀題型）這一步被做掉了。
- **對照組**：`exam/` 子站做法正確——關鍵藏在「💡 關鍵」按鈕後，逐步顯示。
- **修改規格**：
  1. 單元頁（build_html.py 產生的歷屆試題卡與模擬實戰卡）中，「解題核心：…」整行改為預設收合。
  2. 收合狀態顯示一個提示鈕，文案：`🔍 先判斷：這題是哪一類？（點我對照）`。
  3. 點擊展開原本的「解題核心：…（↑ 複習考點 連結一併在展開內容中）」。
  4. 可沿用現有 `sol-btn`／`ts()` 模式或新增輕量 toggle，但**遵守「按鈕後必須緊接其 .sol」的既有慣例**。
  5. 「全部顯示解答」（`revealAll()`）應同時展開所有解題核心（考前速查情境用）。
- **範圍**：全部 11 個單元頁（改版型一處即全站生效）＋確認「待複習與錯題」彙整頁若也印出解題核心，行為一致。
- **驗收**：build 後抽查 3 個單元頁——預設看不到任何「解題核心」文字；點提示鈕可展開；revealAll 全展開；KaTeX 正常；手機寬度按鈕不爆版。

#### A2｜Part 3 考前速查回扣「帶走一句話」（提取而非重讀）
- **現況**：每單元的單元知識地圖裡有一句很好的核心概念句（「帶走一句話」），但 Part 3 考前速查完全沒有回扣它，學生複習結束時沒有提取核心句的機會。
- **修改規格**：
  1. Part 3 開頭（公式速查表之前）新增一個小區塊，標題：`先回想：這個單元帶走的一句話是什麼？`。
  2. 用現有填空／顯示解答互動：學生先自己回想，點按才顯示核心句全文。
  3. **核心句文字必須與單元知識地圖同源**（應在 `soil_maps.py` 或各單元 content 檔中——找到該變數重用，不要複製貼上第二份文字造成雙重維護）。
- **範圍**：版型改一處，全 11 單元生效。
- **驗收**：build 後抽查 3 單元——Part 3 開頭有回想區塊、預設隱藏、點按顯示的句子與該單元知識地圖的「帶走一句話」逐字一致。

> **批次 A 完成後：停下，請老師在手機上實際點過一個單元，確認體感，再 push。**

---

### 批次 B（兩週內）

#### B1｜Part 0 加入「直覺挑戰」——把配分動機升級為認知動機
- **現況**：Part 0 引起動機只有出題趨勢表（配分、年年幾題）。這回答「為什麼值得投資」，但沒有製造「原來我還不知道」的認知落差。
- **修改規格**：
  1. 在 Part 0 趨勢表之前插入一個「直覺挑戰」區塊，互動流程固定三步：**學生先點選自己的直覺答案 → 揭示正確答案（通常反直覺）→ 一句話說明「為什麼會這樣？答案在本單元考點 N」**（附錨點連結）。
  2. 版型做成通用元件（content 檔提供題目資料即渲染），本批先做 3 個高頻單元：**排列組合與機率、三角、直線與圓**。
  3. 第一題（排列組合與機率）直接採用以下內容（素材來自該單元考點 4，已驗證）：
     - 題目：「某疾病盛行率 1%。檢驗很準：有病的人 90% 驗出陽性，沒病的人只有 5% 會誤判陽性。今天你驗出**陽性**——你真的有病的機率最接近多少？」
     - 選項：90%／70%／50%／15%
     - 正解：**約 15%**。揭示文案：「大多數人猜 90%。錯得離譜的原因，就是本單元考點 4（條件機率與貝氏）要教你的事——分母早就換了。」
  4. 三角、直線與圓的挑戰題：**由你起草 2 個候選（各單元），必須是「多數學生會答錯的直覺陷阱」而非冷知識，寫好後停下來給老師挑選與修改，審核通過才進 content 檔。**（三角可考慮 sin(A+B) ≠ sinA+sinB 這類線性直覺誤解；僅供起點，仍須老師定案。）
- **驗收**：3 單元頁 Part 0 出現挑戰區塊、互動三步正常、錨點連到正確考點；其餘 8 單元不受影響（無資料時區塊不渲染）。

#### B2｜置頂導覽列瘦身＋地圖頁遮擋與觸控修正
- **現況**：
  1. 單元頁 sticky header 兩排約 165px 高，捲動時一直佔著螢幕；手機上更嚴重。
  2. 概念地圖頁：地圖上緣會被 sticky header 蓋住。
  3. 概念地圖依賴鏈的箭頭「為什麼需要」說明**只有滑鼠 hover 能看**，手機／平板完全看不到。
- **修改規格**：
  1. 單元頁 header：往下捲動時收合為單排（保留標題＋一個「▾」展開鈕，或標題＋跳至考點），往上捲動或點擊時展開回兩排。收合後高度目標 ≤ 60px。
  2. 同步檢查所有錨點的 `scroll-margin-top`（現為 64px 假設）在收合／展開兩態都不遮字。
  3. 概念地圖頁：修正容器 padding／scroll-margin，讓地圖任何部分不被 header 蓋住。
  4. 箭頭 tooltip 改為 click／tap 也能觸發（hover 保留）。
- **驗收**：DevTools 以 390px 寬度模擬——捲動後 header ≤ 60px；點「跳至考點」落點正確不遮字；概念地圖上緣完整可見；手機模擬下點箭頭能看到「為什麼需要」。**CSS 改動後必查矩陣單元頁 KaTeX。**

> **批次 B 完成後：停下請老師確認再 push。B1 的三角、直線與圓題目未經老師審核前不得 build 進正式檔。**

---

### 批次 C（滾動進行，每次一個單元）

#### C1｜「確認理解」升級：從 1 題同構題變成 2 題微集
- **現況**：每考點末的「確認理解」只有 1 題選擇題，且常與「示範一題」同結構同表面（示範「5 人選 3 人」、確認「8 人選 3 人」，只換數字）。學生答對可能只是模式複製，自評「我懂了」的訊號失真。
- **修改規格**：每考點的確認理解改為固定 2 題：
  - **第 1 題｜辨析題**（練 Selection）：形如「這題該用排列還是組合？題目裡哪個詞告訴你的？」——考「判斷用哪個工具＋依據」，選項含正解與典型混淆項，解析必須指出判斷線索（關鍵字）。
  - **第 2 題｜變情境題**（練近遷移）：與示範一題**結構相同但表面情境不同**（換場景、換敘述方式，不是只換數字），且不得與該考點任何示範題／歷屆題同表面。
  - 保留現有「✓ 我懂了／✗ 待複習」自評與「為什麼」解析按鈕。
- **執行方式**：
  1. 先做**排列組合與機率**一個單元當試點（6 考點 × 2 題 = 12 題），全部題目起草後**停下給老師審**，通過後才 build。
  2. 老師驗收試點格式後，其餘 10 單元每次做一個，同樣「起草 → 審 → build」。
- **驗收（每單元）**：每考點確認理解恰 2 題、第 1 題是辨析型、第 2 題與示範題表面不同；作答／自評／待複習同步功能不變。

---

### 批次 D（選擇性，老師點頭才做；每項獨立）

#### D1｜單元知識地圖標註雙用途（低成本版）
- 現況：開頭那張單元知識地圖資訊密度極高（先備＋六考點卡含公式＋跨單元＋帶走一句話），公式字級小到要點圖放大——它其實是「複習圖」，對第一次讀的學生是過載。
- 規格（先做低成本版）：圖上方加一行使用指引：「**第一次讀：只看 ①→⑥ 的骨架和最下面那句話。考前：回來讀全圖。**」不動圖本身。進階版（預設收合成六格骨架、點開展開全圖）需另行估工，先不做。

#### D2｜強調色規則統一（需老師先同意配色方向）
- 現況：酒紅同時當結構色（標題、導覽、按鈕）與強調色（填空、關鍵），真正的判斷關鍵不突出。
- 規格：定全站規則——酒紅＝結構；另選一個強調色（建議橘系，與現有底色相容）**只用於**：判斷點、易錯條件、關鍵字提示。逐頁套用前先做一個單元的樣張給老師看。**大量 CSS 改動，KaTeX 雷區，務必逐頁目檢。**

#### D3｜概念地圖三視圖編號與預設對齊
- 現況：視圖切換籤顯示順序為「② 概念依賴鏈（預設）／① 大考熱度地圖／③ 跨單元概念網」——編號暗示先看①，預設卻開②。
- 規格：二選一（問老師）：(a) 重新編號讓預設籤＝①；(b) 加一句「建議看法：先①熱度知輕重，再②依賴鏈排順序，最後③概念網看綁考」。

#### D4｜模擬實戰題序打散（交錯練習）
- 現況：Part 2 模擬實戰 11 題按考點 1→6 順序排列（blocked practice），學生可從位置猜題型。
- 規格：單選題內部打亂考點順序（維持難度大致遞增即可），多選、選填同理。在 content 檔調整題序即可，不要動題目內容。

#### D5｜渲染效能（先量測再決定）
- 現況：實測多次出現點擊後頁面凍結 30 秒以上（全頁 KaTeX 渲染量大），低階手機會更痛。
- 規格：先用 DevTools Performance 量測單元頁載入與互動；若確認是 KaTeX 全頁渲染造成，評估「視窗外內容延遲渲染（IntersectionObserver）」方案，**提出評估報告與風險（隱藏內容先渲染是現有慣例，改動會牽動 blank/sol 機制），經老師同意才動工**。

---

## 3. 明確不在範圍內（不要做）
- 不動 `exam/` 子站的任何檔案。
- 不做資料／樣板分離重構、不換框架、不改檔名 ASCII 化（README 的「未來優化」清單本次不碰）。
- 不改網站整體視覺風格與版面架構（Part 0–3 結構、考點卡結構保留）。
- 不新增單元、不改任何歷屆試題題目與詳解內容。
- 批次 B1／C1 未經老師審核的教學文字，一律不得進正式檔。

## 4. 總驗收清單（全部批次結束時）
- [ ] 單元頁所有「解題核心」預設隱藏、可展開、revealAll 連動（A1）
- [ ] 全 11 單元 Part 3 有「帶走一句話」回想區塊且與知識地圖同源（A2）
- [ ] 3 個高頻單元 Part 0 有老師審核過的直覺挑戰（B1）
- [ ] 手機模擬 390px：header 收合 ≤ 60px、錨點不遮字、地圖不被蓋、箭頭可點出說明（B2）
- [ ] 試點單元（排列組合與機率）確認理解 = 每考點 2 題微集且經老師審核（C1）
- [ ] 每次 CSS 改動後，矩陣單元頁 KaTeX 目檢通過
- [ ] 每批次 push 前均經老師確認
